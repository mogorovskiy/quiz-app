package com.mogorovskiy.quiz.executor.impl;

import java.util.Scanner;

public class PlayTrueFalse {
/*
    private void handleTrueFalseTest(TrueFalseCard card, Scanner scanner) {
        System.out.println("Enter true (true/false):");
        boolean userAnswer = scanner.nextBoolean();
        scanner.nextLine(); // Consume newline

        if (userAnswer == card.isCorrectAnswer()) {
            System.out.println("Correct!");
        } else {
            System.out.println("Incorrect. Correct answer is: " + card.isCorrectAnswer());
        }
    }*/
}
